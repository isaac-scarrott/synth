.DS_Store
*.swp

# Swift / SwiftPM (app/)
app/.build/
app/.swiftpm/
app/Package.resolved

# Local scratch
*.log

# Globally-ignored on this machine, but tracked in this repo
!CONTEXT.md
!docs/
!docs/adr/
!docs/adr/**

# MCPInstaller writes this into every worktree, and its `SYNTH_WORKTREE` is an absolute path
# to *that* worktree — unlike .mcp.json, which only names the shared server install.
/opencode.json
