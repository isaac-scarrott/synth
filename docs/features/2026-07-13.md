# 2026-07-13

## ⌘D closes the current context (both designs + native app)

⌘D now closes "the current context" — resolved by the same rule ⌘K, ⌘T and ⌘N already use
(`contextRow()`): the focused sidebar row when the sidebar's keyboard ring is showing, otherwise
the session open in the content pane. From the main window it acts on the current session; from
the sidebar it is exactly `d` — same `requestDelete` flow, so an idle unowned session closes
immediately and anything busy, browser-owning, or a branch/workspace drops into the ⌘K confirm
frame. Not a new close path: one keystroke into the existing one, reachable without first handing
focus to the sidebar (the gap feedback flagged — closing the session you're looking at required
⌘0 first).

Surfaced in the ⌘? sheet (General → "Close current session") and as the key hint on the ⌘K
Session "Close" action. Guarded like its siblings: no-op in Settings, inert while the palette is
open, plain `d` in the sidebar unchanged, ⌘⇧D untouched.

Verified in the browser over CDP: busy open session → "Close dev server?" confirm; idle sidebar
row under the ring → removed with no prompt; branch row under the ring → "Remove
feat/command-menu?" confirm. Subset invariant held (diff = title + big-picture's extra session
rows).

Natively, ⌘D is a real menu item — File > Close Session — calling `store.closeContext()`
(Navigation.swift), which mirrors `contextRow()` (`keyboardActive ? cursorRef : openSession`)
into the existing `requestDelete` path; the ⌘? sheet and the ⌘K Close item's key hint follow.
Verified against an isolated instance over the control socket (9/9): idle open terminal closed
by ⌘D with no confirm, cursor fell to the branch row, a second ⌘D raised "Remove master?",
Esc cancelled with nothing else lost — screenshot captured via `automation.screenshot`.

## Session Close always confirms — the idle skip is gone

Feedback: ⌘D on an idle Claude Code session just closed it, no prompt — surprising, because unlike
an idle terminal or browser tab, an idle agent session still carries a conversation you can't get
back once the row is gone. The shipped rule (this file, above) special-cased "idle and unowned" to
skip the ⌘K confirm frame entirely; that carve-out is removed. Every Close — `d`, ⌘D, the ⌘K palette
item, and the kebab menu — now always drops into the confirm frame, busy or not, owned browsers or
not. Red still only wears while busy (ADR-0013's "red means loss, not disk" tracks the color, not
whether the frame appears at all).

`sessionNeedsConfirm` / `closeNeedsConfirm` / `skipsConfirm` deleted outright (`working.html`,
`big-picture-design.html`, `Store.swift`, `Navigation.swift`, `Palette.swift`, `Menu.swift`,
`RowMenu.swift`) rather than left as an always-true stub — nothing calls the idle-skip path anymore.

## synth-app MCP server — approval-gated worktree creation + handoff

A second bundled MCP server, `synth-app` (`mcp/app-server.mjs`), sibling to `synth-browser`:
where the browser server drives a page, this one drives Synth itself. First (and so far only)
tool: `worktree_create(branch, base?, handoff?)` — its description tells agents to call it
PROACTIVELY whenever they're asked to start work that doesn't belong on the current branch,
so unrelated work gets its own branch, checkout and sessions instead of piling onto the one
in flight.

Decisions worth remembering:

- **Two servers, not one.** App control and browser control stay separate processes with
  separate registry entries, because they carry different risk and different defaults: the
  browser server ships ON, the app server ships OFF. A new global Settings section ("MCP
  servers", global scope only — native-first like the notification-sounds section) holds the
  two toggles; flipping one re-syncs every managed worktree's `.mcp.json` / `opencode.json`
  immediately, and *disabled means removed* — the entry is reconciled out of the configs, so
  agents never even see the tools. Both servers share discovery/control plumbing via
  `mcp/shared.mjs` (extracted from `server.mjs`, no behaviour change).

- **Every mutating verb is approval-gated in the app, not the agent.** `worktree_create`
  sends `app.worktreeCreate` over the control socket; the connection's thread parks on a
  semaphore (never main) while Synth shows a native modal — who's asking, workspace chip,
  branch, base, and whether a handoff will follow. Enter creates, Esc / "Not now" / backdrop
  declines. The user's answer is the tool result: approve returns the planned checkout path
  (materialisation continues in the background, failures surface as the usual worktree error
  toast); decline tells the agent to continue on the current branch or ask the user — not to
  retry unprompted. The window is 4 minutes app-side inside a 250s socket timeout
  server-side, so the agent always reads a real answer, not a dead pipe. A branch that's
  already a Synth row answers "exists" immediately, prompt-free — idempotence over ceremony.

- **Handoff rides the feedback loop's delivery path.** With `handoff` text, the approved
  worktree skips the session template and instead spawns one Claude session seeded with the
  brief — `seedFeedbackAgent` generalised to `seedAgent` (mount-for-a-beat PTY boot,
  liveness-gated delivery, same security stance: only a supervisor-confirmed-live agent is
  ever handed text). Without `handoff`, the normal template applies and the work stays with
  the requesting agent.

- **Existing-vs-new branch is decided inside the background git op**, not on the approval
  click's main-thread hop: a branch with a worktree is reused, an existing local/remote
  branch is checked out (`git worktree add` DWIMs remote-tracking names), anything else is
  cut off `base` (repo HEAD when omitted).

- **Headless drivability.** New automation verbs `automation.agentPrompts` /
  `automation.agentPromptResolve` map 1:1 onto the sheet's calls, and harness suite
  `t8_appmcp.py` proves the whole loop end to end: default registration (browser on / app
  absent), refusal while off, per-process enablement via the NSArgumentDomain
  (`-synth-mcp-app '<true/>'` — no defaults pollution), prompt → approve → real checkout +
  seeded Claude row, decline → nothing created, "exists" short-circuit, and stale-entry
  removal on relaunch. 17/17 checks pass; `lib.py` gains a `SYNTH_APP` env override so a
  worktree's build can be gated without redirecting other checkouts' harness runs.

## Synth 0.3.0 — agents can ask for their own worktree

0.3.0 (build 191) is a minor, not a patch: it carries the two capability changes above. ⌘D closes
the current context, and the `synth-app` MCP server lets an agent request a worktree behind a native
approval prompt. The second is the reason the version moves — Synth now exposes a control surface
that agents drive, so the app is no longer only something you drive by hand.

The app server ships OFF. A user upgrading to 0.3.0 gets the tool installed but disabled, and has to
opt in from Settings > MCP servers. Nothing an agent can do changes until they do, which is the
right default for the first release where an agent can mutate the workspace.

Delta economics held: six deltas, 3.9MB total, against a 132MB full download. Installed copies on
171/181/185 all get a few-MB update. This is the first release where `app/releases/` proved its keep
— three prior zips on disk meant `generate_appcast` could diff against every build still in the
wild.

Verified as an outsider, not by exit code: anonymous download of the public `Synth.zip`, Safari's
quarantine bit set, `spctl` returning `accepted / source=Notarized Developer ID`, the staple
validating offline, the bundle reporting 0.3.0 / 191, and all nine appcast enclosures (three full
zips + six deltas) carrying an `edSignature`.

Housekeeping the release forced: `app/harness/agents/__pycache__/lib.cpython-314.pyc` was tracked in
git with no ignore rule, so every harness run rewrote it and left the tree dirty — which
`release.sh` refuses. Untracked, and `__pycache__/` / `*.pyc` added to `.gitignore`. It would have
blocked every future release the same way.

## synth-app MCP approval moves into ⌘K (supersedes the 0.3.0 modal)

Feedback on the just-shipped 0.3.0 flow: the agent-worktree approval prompt was a modal sheet, the
one place in the app where an action didn't happen inline as a ⌘K text frame — Palette.swift's own
header comment already states the rule it broke ("create / delete / confirm happen inline as text
— never a modal"). Same violation `d`-to-remove and ⌘D fixed for sidebar deletes (ADR-0013) applied
here too, just missed in the MCP server's own PR.

Fixed by routing the prompt through the existing `mode: .confirm` frame machinery instead of a new
one: `PaletteModel.confirmAgentWorktree(_:)` builds a two-item frame — "Create `<branch>`" / "Not
now" — worded like the sheet's copy (who's asking, base, whether a handoff follows) but as the
placeholder hint rather than laid-out fields. `AppStore.presentAgentPrompt` opens the palette (or
reuses it, discarding whatever frame was showing — the same interrupt the modal already did) and
resets its stack to `[root, confirmFrame]`; `presentedAgentPromptID` tracks which prompt currently
owns that frame.

The one behavior worth preserving exactly: a prompt can't just vanish. `closePalette()` now checks
`presentedAgentPromptID` and, if set, calls `resolveAgentPrompt(approved: false)` before clearing —
so Esc, ⌘K, and a backdrop click all decline, same as before. Resolving (from either palette item,
the decline-on-close path, or the automation verb) chains to the next queued prompt via
`advanceAgentPromptPresentation`, reopening the palette on it if the queue isn't empty — reproducing
the old reactive-modal behavior (`agentPrompt` = `agentPrompts.first`) without a computed property
driving an overlay.

`AgentWorktreeSheet` (Dialogs.swift) and its `ModalBackdrop` overlay are deleted; `DialogFrame` /
`Field` stay in use by the other sheets (feedback, add-project, new-worktree). No change to the
automation verbs (`automation.agentPrompts` / `automation.agentPromptResolve`) or the MCP server
itself — only how the answer gets in front of the user.

## Fix: a hidden, stationary pointer could steal keyboard nav

Feedback: opening ⌘K sometimes highlighted a row halfway down the list, and the sidebar's
keyboard selection ring could vanish mid-navigation — both with the mouse never touching the
trackpad. Root cause: `NSCursor.setHiddenUntilMouseMoves(true)` (fired on every keystroke) hides
the pointer but leaves it parked at its last real position, and SwiftUI's `onHover`/
`onContinuousHover` re-fire from hit-testing whenever a view is laid out under that position —
the palette list scrolling under keyboard nav, or the sidebar tree scrolling to the nav cursor,
both count as "hover" with zero actual mouse movement.

Added `AppStore.pointerStale` (`Store.swift`), true from the same keystroke that hides the
pointer until a genuine `mouseMoved` event clears it. Three hover callbacks that mutate real
state off a stale hit-test now check it first: the ⌘K row hover (`Palette.swift`), the sidebar's
ring-dismiss-on-hover (`Sidebar.swift`), and the notification deck's hover-to-fan
(`NotificationDeck.swift` — a phantom hover could pop the deck open uninvited). Left the deck's
remaining purely-cosmetic `.onHover` highlight toggles (RowMenu, BrowserPane, Dialogs,
SettingsPane) alone — same latent quirk, but no wrong action results, just a rare mistimed tint,
and gating them needs new store plumbing on several small view structs for little payoff. Matches
working.html, which never had this bug: its hover is a literal `mousemove` DOM listener, not a
hit-test callback that layout alone can trigger.

## Browser device mode (⌘⇧M): the page in a device frame (both designs)

Feedback: the browser had comment mode, inspect (DevTools), and open-in-default-browser — but no
way to see the page as a phone or tablet would render it. New fourth toolbar mode, **device
mode**: a device-mobile toggle next to comment mode that puts the page inside a hardware frame at
a real device viewport.

The strip under the toolbar carries the fleet — the popular current devices, deliberately
spanning the smallest viewport in circulation to the biggest, so a layout is checked at both
extremes rather than one convenient middle: iPhone SE (375×667), iPhone 16 (393×852, the
default), iPhone 16 Pro Max (440×956), Galaxy S25 Ultra (412×952), iPad mini (744×1133), iPad
Pro 13″ (1032×1376). Dimensions are CSS-viewport points, not hardware pixels — the numbers a
media query actually sees — shown live in a readout beside the chips, with a rotate button that
swaps them for landscape.

The frame lays out at true viewport size and scales **down** to fit the pane, never up, so small
devices stay life-size when there's room and an iPad Pro shrinks to fit; a ResizeObserver on the
stage re-fits on pane/sidebar/window resize. Frame chrome follows the hardware: dynamic island +
home indicator on current iPhones, punch-hole camera on the Galaxy, classic bezel on the SE,
uniform pad border on iPads.

It's a mode like the others and composes with all of them: it survives navigation (like comment
mode), DevTools docks below the framed page, and a comment in device mode lands on the device's
*screen* — clicks on the stage backdrop around the frame don't drop a card. ⌘⇧M toggles it
(Chrome's own device-toolbar chord), the ⌘K Page group gains Enter/Exit device mode, and the ⌘?
sheet lists it under Browser. Home ("go to") pages have nothing to frame, so the button disables
there like comment mode. Native port pending — in the real app the CEF surface renders inside the
frame with `Emulation.setDeviceMetricsOverride` sizing the viewport.

## Native port: browser device mode (⌘⇧M), CDP-emulated viewport

The device mode shipped in the designs above now runs in the app. The chrome is a faithful
SwiftUI port — `DeviceMode.swift` carries the fleet catalog + per-kind frame metrics, and
`BrowserPane.swift` gains the bar toggle, the chip strip (live W × H readout, rotate), and the
scale-to-fit stage/frame — but the viewport is *real* where the mock's was a skeleton: a
`DeviceEmulator` attaches a CDP client to the session's page target (the CommentMode pattern) and
drives `Emulation.setDeviceMetricsOverride {width, height, deviceScaleFactor, mobile: true,
scale}`, so the page actually lays out at 393×852 @dpr 3 and input maps exactly — Chrome's own
device-toolbar mechanism. Proven live: a test page reported its emulated `innerWidth×innerHeight`
per device/rotation, corner-targeted clicks landed within 1px, and the override survived
navigation; `scale` composes with the frame's fit-scale in CEF exactly as hoped, no fallback
needed. Exit clears the override; `cdpPort == 0` (the WKWebView hedge) degrades to frame-only.

One deliberate deviation from the mock: no "9:41" status row — the mock draws it because a
skeleton page looks bare, but natively it would either steal viewport height or make the dims
readout lie; the full screen is the live, truthful viewport (matching Chrome's device toolbar).
⌘⇧M rides the existing browser-chord switch, the ⌘K Page group and ⌘? Browser group carry the
verb, and mode state lives on the session controller so it survives pane swaps, like DevTools.
An independent fidelity audit passed all checks (fleet, frame metrics, strip, behavior, chord,
icons, hygiene) with zero blockers.
