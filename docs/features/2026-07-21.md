# 2026-07-21

## The sidebar's split band survives switching branches / workspaces

The sidebar echo of a split (012) is per-branch: a branch whose remembered layout binds ≥2
sessions shows its member band even while some *other* branch or workspace is open on the
content surface. The current branch's band keeps echoing the on-screen durable tree; every other
branch's band reads its stashed `Branch.layout` — the same tree a member click restores (014).

**Why.** The layout was already owned by the branch and sticky (014): switching away stashed the
split and clicking any member brought it back intact. But the band — the visible "the split is
still here" — dissolved the moment another branch's layout took the surface, so the sidebar read
as if the split had been destroyed, and the only way to learn otherwise was to click back in.
Feedback (feedback/split-switching-branches-or-workspaces): the band should keep telling the
truth about the branch's remembered arrangement, not about what happens to be on screen.

Consistency follow-through: `inSplit` / `unsplitSession` understand a background branch's
remembered split too, so a band tile's kebab still offers Unsplit and dragging a tile out to the
plain sidebar still leaves the split (013) without having to open the branch first. On-screen
behaviour (including transient zoom) is untouched. App-only for now — `working.html`'s echo still
renders a single band for the current branch; the mock catch-up is a follow-up.

## Split focus is a top-edge bar in the mark's colours

The active pane in a split is marked by a 2px bar across its top edge — charcoal `#1e2126` on
light, champagne `#eee0cd` on dark (the Synth mark's own pair, via a new `--focus` token) — 
sweeping in from the left while the previous pane's bar fades (015). Both ends are inset by
`--radius-app` with soft caps so the bar always clears the shell's rounded corners (top-right;
top-left too once the sidebar is collapsed), keeping every pane's bar identical wherever it sits.

**Why.** Feedback (feedback/changing-focus): the copper ring (004 §4) fenced the whole pane, sat
on top of the content, and read loud for a state that is always true. Ten alternatives were
explored in a temp mock (`focus-ideas.html`) — header tint, dimming, title dot, raised card,
edge spine, corner brackets, surface ring, scrim, live cursor — and the top-edge bar won: one
edge instead of four, zero ink on the content, the browser-tab underline idiom flipped up. The
focus colour gets its own `--focus` token (not `--accent`) so the signal can move independently
of the structural accent; in dark the two coincide on the champagne mark. Superseding note only —
the ring CSS is replaced in both design files; the app port rides with the split-focus click fix
(the pane's SwiftUI tap gesture never fires over the AppKit terminal view, so body clicks don't
activate — first-responder change is the natural trigger).

## Toasts anchor to the shell's corner and drain a countdown bar

The in-app notification deck moves from the content area's left edge (`sidebar-width + 22px`) to
the bottom-left corner of the whole shell — `left: 22px`, over the sidebar. One fixed home
whatever the sidebar width, collapse state, or split layout does; the 22px inset clears the
shell's rounded corner, mirroring the focus bar's corner-clearing inset up top. A toast that
will dismiss itself (kind `done`) now carries a countdown bar along its bottom edge — the split
focus bar's grammar reused as a clock: 2px, the `--focus` token (charcoal on light, champagne on
dark), ends inset by the card radius with soft caps. It drains toward its left origin over the
toast's 6s lifetime; dismissal fires on the bar's `animationend`, so the bar and the timer can
never disagree. Hovering the deck pauses the drain (pure CSS `animation-play-state`) — reading
is not ignoring. Sticky toasts (needs-input, error) ask for an answer, so they carry no bar and
never self-dismiss.

**Why.** Feedback (feedback/toast-notifications): anchored to the sessions area, the deck moved
whenever the sidebar resized or collapsed, and read as belonging to the content pane rather than
the app. And a self-dismissing toast gave no hint it was about to leave — the drain shows the
remaining time at a glance, in the same visual language the split focus bar established.
Standalone exploration in `toast-notifications-design.html`; landed in both design files.

## Unfocused notifications raise the toast as well as the banner

The in-app toast deck is no longer the frontmost-only surface. A background session's
needs-input / error / done now raises its deck toast whether Synth is focused or not; focus
only gates Notification Center, which posts *on top of* the deck when Synth isn't frontmost
(`routeTransition`: the deck is unconditional for non-open sessions, `toNC` adds the banner).
The open session still never toasts in-app, and still reaches Notification Center unfocused
(open ≠ seen). Losing focus joins hover as a brake on the done toast's drain clock (the
countdown bar above): raised — or still up — unfocused, the toast waits in the deck fully
banked, and gets its six seconds of being seen after focus returns before dismissing.

**Why.** Feedback (feedback/toast-and-native-notifications): when Synth isn't focused it
should still display the toast as well as the native notification. A Notification Center
banner slides by while you're in another app and leaves no trace; the deck previously stayed
empty on that path, so returning to Synth showed only the unread bullet. Now the banner
catches the eye in the moment and the toast is standing in the deck when focus returns —
ranked, clickable, ⌘↩-jumpable as ever. `NotifRoute` and the `automation.notifRoute` seam
("deck" / "nc") now pin the *focus rule* (deck-only vs deck + NC) rather than either/or
surfaces. Mock comment-only in both design files (a browser page is always "focused", so the
mock's deck behaviour is unchanged); verified against a driven instance via `automation.key`
⌥⇧N / ⌥⇧D + `automation.notifs` — toast standing with `active:false`, done toast still
standing at t+9s unfocused.

## Synth 0.7.0 shipped (build 293)

Second release on top of 0.6.0's pane-splitting foundation, gathering this window's split and
notification refinements: the corner-anchored toast deck with the draining countdown bar and its
hover / lose-focus brakes, the raised-when-unfocused toast, the split-focus top-edge bar with
click-to-focus, the per-branch split band surviving branch/workspace switches, the copper reorder
drop line (sidebar + Settings templates), single-branch split enforcement at the interaction layer,
and the keyboard-driven two-pane Changelog sheet. Minor bump — the toast/countdown and split-focus
work are new user-facing surfaces, not just fixes.

Notarized, stapled, 15 deltas (23MB) against a 137MB download; verified credential-less (spctl
accepted / Notarized Developer ID, staple valid, appcast newest `Synth-0.7.0.zip` at
`sparkle:version` 293 with `edSignature` on every enclosure) and installed to `/Applications`.

## Crash reporting actually reports: Ghostty's Breakpad no longer eats every crash

A user reported four crashes; PostHog held one `app_crashed` event ever, and that one was a dev
build. Analytics itself was healthy — five non-dev installs sending lifecycle events — so the gap
was the crash path.

The `.ips` from the affected 0.6.0 install named the culprit: the triggered thread was the idle
`NSApplication run` loop, while a side thread sat in
`google_breakpad::ExceptionHandler::WaitForMessage` → `exc_server` → `__kill`, and a second thread
carried Apple's `SOME_OTHER_THREAD_SWALLOWED_AT_LEAST_ONE_EXCEPTION` marker. Breakpad had taken the
fault on the task's Mach exception port and killed the process itself, so neither Apple's report nor
ours could name a cause.

The Breakpad is ours by accident: `GhosttyKit.xcframework` statically links sentry-native
(`_sentry_init` is reachable from `ghostty_init`; 116 breakpad symbols in the binary), and the
vendored artifact is a pinned prebuilt from a fork — there is no runtime switch to turn it off.
Mach exception ports preempt POSIX signal delivery, so from the moment the first terminal surface
came up, `CrashReporter`'s signal handlers were dead code and the marker file was never written.

Fix is ordering, not force. `GhosttyApp.shared.start()` now runs at the top of
`applicationDidFinishLaunching`, before `Analytics.bootstrap`, so Breakpad claims the port first and
PostHog's PLCrashReporter (`errorTrackingConfig.autoCapture`, plus `autocapture_exceptions_opt_in`
on the project) layers on top and chains back to it. Both handlers now coexist —
`exception_server_thread` (`PLCrashMachExceptionServer.m:681`) and Breakpad's `WaitForMessage` both
present in a live sample — instead of one silently winning.

Kept the homegrown `CrashReporter` as the signal-level backstop: a forced `SIGSEGV` still writes the
marker and reports `app_crashed` on the next launch, verified end to end against the project.

## Synth 0.7.1 shipped (build 294): the Bluetooth TCC hard-abort

With crash reporting finally intact (above), the 0.7.0 release's own `.ips` named the next one: a
`SIGABRT` on a `default-qos` thread inside `__TCC_CRASHING_DUE_TO_PRIVACY_VIOLATION__ →
abort_with_payload`. TCC's detail was explicit — the app touched Bluetooth with no
`NSBluetoothAlwaysUsageDescription` in Info.plist, so the system killed it. Synth's own code never
mentions CoreBluetooth (grep is clean); the probe comes from an embedded engine — Chromium's Web
Bluetooth via CEF, or libghostty — and the first time it reaches for the radio the whole app aborts.

Fix is a declaration, not code: `write_info_plist` in `lib.sh` now emits
`NSBluetoothAlwaysUsageDescription`, so the generated bundle satisfies TCC and the probe fails
softly instead of aborting. Patch bump — two crash fixes on 0.7.0, no user-facing surface change.
