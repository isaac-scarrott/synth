# 2026-07-24

- **Two perf follow-ups: feedback submit no longer freezes, palette builds its list once** — the
  feedback sheet's submit resolved its `feedback/<slug>` branch name via `GitService.allBranches`
  (a `git for-each-ref` over heads + remotes) synchronously on the main actor — hundreds of ms of
  freeze on a big/cold repo. Now the "On it" toast fires immediately and the name resolves off-main
  through the existing `runGit` chain (same shape as `addBranchFromPrompt`), with the pending row
  added when it's known. Separately, `PaletteOverlay` rebuilt its item list three times per render
  (`.isEmpty`, `.count`, and the `rows` getter, each re-running build + fuzzy + sort); it's computed
  once now. Both behaviour-preserving (palette open/filter/No-results/close driven); `working.html`
  untouched.

- **Scaling-perf pass: persistence, palette/drag scans, and browser-open moved off the main-thread hot
  path** — a `sample`-profiled + agent-audited sweep of the heavy subsystems (the same session that
  killed the autofill walk below) fixed three main-thread hazards that worsen with session count.
  (1) **Persistence off-main:** `saveNow()` built the whole tree into Codable structs, JSON-encoded
  it, and wrote a backup + primary atomically — synchronously on the main thread, on every ⇧J/⇧K
  reorder keystroke, every split, and every 4s autosave tick. Now the encode + diff + atomic write
  run on a serial background queue that owns `lastBytes`; `snapshot()` stays on the main actor; the
  terminate path uses a synchronous `flush()` so quitting never drops the last edit (verified: async
  reorder persists, and a reorder 0.3s before SIGTERM still lands). Reorder main-thread cost fell
  28ms→6ms on the 30-session tree, and it no longer scales with total state. (2) **O(N²)→O(N):** the
  ⌘K root/session search rebuilt every session's "workspace / branch" context by calling
  `store.branch(of:)`/`workspace(of:)` — full-tree scans — per session, per keystroke; now the
  enclosing loop's ws+br is threaded through, and `Layout.pairTarget` builds a `sessionID→branchID`
  map once per drag frame instead of scanning per candidate row. (3) **Browser open non-blocking:**
  `BrowserPane` built the engine synchronously inside its view body, so a CEF spawn (which pumps the
  main runloop) froze the app mid-open; the pane now paints a placeholder card and bootstraps the
  engine on the next runloop turn (reusing the non-focused-create deferral), with `controller(for:)`
  and every automation caller left on their synchronous contract. All three are behaviour-preserving
  and were driven-verified; `working.html` is untouched.

- **Every native text field focuses instantly — the password-autofill key-view walk is killed app-wide** —
  the same heuristic the ⌘K palette had to escape into its own `NSPanel`
  (`_showPasswordAutoFillIfNecessaryForView` → `NSHostingView._recursiveGatherAllKeyViewCandidates`
  → `FocusNavigator.allItems`, a per-node uncached protocol-conformance walk of the field's whole
  window) fired for **every** other field too: focusing them walked the entire main-window tree
  (sidebar rows + open panes) on the main thread. Profiled on the 30-session / 10-branch tree with
  `sample`: inline rename (`r`) cost **412ms** of main-thread CPU per focus and the feedback field
  (⌘⇧F) **358ms** — both dominated by a ~3600-sample gather subtree, scaling with the tree so it
  worsened as sessions piled up. Synth has no native password/username fields (rename, feedback,
  settings, palette, the browser omnibox are all plain text; web-content autofill lives inside
  CEF/WKWebView, untouched), so the heuristic only ever burned the walk and showed nothing. Fixed by
  replacing `-[NSAutoFillHeuristicController _showPasswordAutoFillIfNecessaryForView:withCompletionHandler:]`
  with a no-op at launch (`AutoFillSuppression.install()` from `applicationDidFinishLaunching`),
  guarded on the private symbols still existing so a future macOS just restores the old (slow) walk,
  never a crash. After: **rename 412ms → 3ms** (137×) and **feedback 358ms → 90ms** (residual is the
  sheet's own render, gather gone) — both now under the RAIL <50/<100ms interaction bars. One source
  fix covers rename, feedback, settings, the omnibox and the add-project sheet at once, and immunises
  future fields, where the palette needed a whole `NSPanel` to escape the same walk. Verified
  behaviourally: the palette still filters as you type (14→2 items) and an A/B on the same binary
  (autofill kept vs suppressed) showed identical field behaviour — the change is a pure perf no-op on
  editing, so `working.html` is unaffected.

- **⌘K palette opens instantly again — the ~240ms key-view stall is gone (native app)** — making
  the palette's search field first responder fires AppKit's password-autofill heuristic, which
  gathers every focusable key-view in the field's window (`_showPasswordAutoFillIfNecessaryForView`
  → `NSHostingView._recursiveGatherAllKeyViewCandidates` → `FocusNavigator.allItems`, a per-node
  uncached protocol-conformance walk). The key-view loop is per-window, so while the palette was a
  SwiftUI overlay on the main window that walk crossed the whole app tree — sidebar rows + every open
  pane — and cost ~250ms, widening as a session piled up. Fixed by hosting the palette in its own
  borderless child `NSPanel` (`PaletteHost` / `PalettePresenter`, driven by `store.palette`): the
  field's first-responder window is now the panel, whose tree is just the palette, so the gather sees
  a handful of nodes instead of hundreds. Measured on a 30-session / 10-branch tree with `sample`:
  the gather went from **1174 samples in the stack (~250–290ms of main-thread CPU per open) to zero**;
  the felt main-thread drift on open dropped from ~250ms to ~40ms. It's a standalone `NSPanel`, not a
  `addChildWindow` child, so the main window stays the *main* window (traffic lights keep their colour)
  while the panel merely takes *key* for text input — verified `rootIsMain` stays true with the
  palette open. Focus held over 25/25 driven opens on both empty and loaded trees; typing/filter,
  arrow-nav + Enter to run, Esc, ⌘K toggle, kebab drill and ⌘N all still drive the panel. Pure
  hosting change — `PaletteOverlay` and every palette interaction are untouched, so `working.html`
  is unaffected. Completes the "left standing" note on the focus fix below.

- **⌘K reliably focuses the palette input again** — opening the palette set focus on its
  SwiftUI `TextField` via `@FocusState` in `.onAppear`. Making any text field first responder
  triggers AppKit's password-autofill heuristic, which asks SwiftUI to gather every focusable
  key-view in the window (`_recursiveGatherAllKeyViewCandidates` → `FocusNavigator.allItems`), a
  main-thread traversal with an uncached protocol-conformance lookup per node. `sample` put it at
  ~240ms per open, and it scales with the view tree — so a long-running session that had piled up
  sessions and panes widened the window during which the `@FocusState` set silently never applied,
  leaving the input unfocused and dropping the keystrokes into the pane underneath. That regression
  grew as the app got more loaded, which is why it "used to work." Fixed by bridging the input from
  AppKit (`PaletteQueryField`, an `NSViewRepresentable`) and grabbing first responder imperatively
  with `makeFirstResponder` — immune to the starved SwiftUI focus cycle. Live filtering, the
  dash-spaces transform, frame-drill refocus, and rename-seed select-all are preserved. Driven-app
  verified across empty and heavily-loaded trees. Left standing: the ~240ms key-view traversal
  itself is inherent to SwiftUI computing the window's key-view loop on focus; killing it would mean
  isolating the palette in its own window or pruning focusable candidates — a larger change, not
  bundled here.

- **Synth 0.10.0 shipped (build 331)** — a minor bump rolling up everything since 0.9.1: the flat
  two-tab Settings surface with project settings layered on the shared default, the simplified Add
  project flow (default branch only, always the folder picker), the cool near-white light terminal
  with Claude Code's theme following Synth's appearance, and the ⌘K palette focus fix above. Both
  artifacts notarized and stapled; the app validates on its own dragged out of the disk image, and
  the appcast carries build 331 with an EdDSA signature on every enclosure.

- **Synth 0.10.1 shipped (build 342)** — a patch release rolling up the responsiveness pass that
  landed after 0.10.0's tag: hosting the ⌘K palette in its own `NSPanel` so its open no longer pays
  the ~240ms whole-window key-view walk, no-op'ing the password-autofill heuristic app-wide so every
  native text field (rename, feedback, search) focuses instantly, and moving state persistence, the
  palette/drag scans (O(N²)→O(N)), browser-open, and feedback's branch-name resolution off the
  main-thread hot path. No new surface — purely making large sessions stay instant, which is the
  product's first principle. Both artifacts notarized and stapled; the app validates on its own
  dragged out of the disk image (spctl accepted / Notarized Developer ID on the quarantined dmg and
  zip), and the published appcast carries build 342 at `sparkle:version` with an EdDSA signature on
  the full download and every delta. Landing-site links point at the stable `Synth.dmg`/`Synth.zip`
  aliases, so no `synth-site` publish was needed.

## Experimental Tabs — the native port lands (off by default)

The single-strip Tabs view mode decided in the 2026-07-23 mock (ADR-0014's 2026-07-23 amendment,
`CONTEXT.md` **Tab**) is now implemented in the native SwiftUI app, gated behind the same
off-by-default global preference so a tabs-off build is byte-for-byte today's behaviour. It is a
faithful port of `working.html`, not a redesign — the store stays `branch → pane-tree → sessions`,
and every tabs-gated path keys off `AppStore.tabsMode`, so flipping the toggle re-renders the same
store losslessly with no migration.

**The flag.** `AppStore.tabsMode` — a persisted global preference mirroring the theme/sound prefs
(`UserDefaults`, key `synth-tabs`), off by default. An `@Observable` read, so the sidebar and content
re-render the instant it flips. Settings gains an **Experimental → Off/Tabs** segmented control
(global scope only, mirroring the Appearance control).

**Two-deep sidebar.** With tabs on, `BranchRow` drops its disclosure chevron, keeps its Reveal shut
(session rows + the split echo band leave the tree), and forces the branch roll-up visible even while
nominally expanded — the branch's only per-session signal. `peeking`/`isActivePill` are made
tabs-aware so the branch keeps its "you are here" pill when its sessions no longer show below it.

**One strip per branch (`TabStrip.swift`).** The horizontal twin of the sidebar rows: the branch's
sessions in order, the on-screen split's members bonded into a contiguous cluster at their first
member's position (styled like the sidebar's split band — rounded chips on `raised`, active member
accent-ringed), the rest lone tabs, then `+` and a right-aligned relocated PR chip. Selecting a lone
tab full-screens it (the durable split stays shown as the bonded cluster, stashed behind the
full-screen); selecting a member returns to the split. The per-pane header (`PaneHead`) is hidden in
tabs mode; when the sidebar is collapsed the strip carries the expand toggle past the traffic lights.
Reuses the sidebar's exact indicator chrome (`SessionIcon`/`StatusIndicator`/`OwnedIndicator`) — an
agent-owned browser wears its owner-mark; unread lands as a blue dot on the tab icon.

**Keyboard.** `⌘⇧[`/`⌘⇧]` and `⌃⇥`/`⌃⇧⇥` step the branch's tabs (`cycleTab`), `⌘1–9` selects the Nth
tab (`focusPane` tabs-aware), `⌘W` closes the active tab through the same confirm path as `⌘D`. The
⌘? shortcuts sheet inserts a **Tabs** category after General and rewrites the Split-layout rows tabs
changes (⌘⇧arrow becomes "Send tab", ⌘⇧U becomes "Merge pane"), so the sheet always matches reality.

**Drag.** A tab's unified drag mirrors the sidebar's `SessionRowDrag` on the horizontal strip:
over a pane → split it at the pointer; squarely over another tab → pair the two into a split; between
tabs → reorder the branch's sessions (horizontal twin of the reorder line); a split member dragged
back to the strip leaves its split. Reuses the same store ops (`dropZone`/`performDrop`,
`pairTarget`/`performPair`, `unsplitSession`) — which already enforce "a session lives in one leaf,
never duplicated" — plus a new horizontal `reorderTab`/`tabReorderLine`.

All slices runtime-verified by driving the built app (split-with-cluster, lone-tab-with-stashed-split,
settings toggle, `⌃⇥` cycling, the shortcuts Tabs group) and the tabs-off default confirmed unchanged.

## A worktree Delete can no longer fire from one stray keypress

The Command menu's confirm frames opened with their **first item pre-selected** (`push` → `query=""`
→ `didSet activeIndex=0`), and the native `confirmRemoveBranch` listed **"Delete worktree" (rm the
folder from disk) as that first item** — an order the `working.html` source of truth never had (it
leads with the non-destructive "Remove from sidebar"). The native port had silently inverted the two.
Net: opening the remove-worktree confirm and pressing **Enter once** deleted the checkout from disk,
and that confirm is reachable by `⌘D` on a branch row (the 2026-07-13 "close current context") as well
as ⌘K. This is not hypothetical — a real `holibob` worktree (`redesign-improvements`) was deleted this
way, renamed aside to `.deleting-…` by `detachWorktree` while its running processes carried on from
memory. (Recoverable precisely because phase-one is a rename, not an rm; the background rm / next-launch
`sweepDetachedWorktrees` is what makes it permanent.)

Fix, both layers: **a `.confirm` frame never opens on a destructive row.** The selection starts on the
frame's first non-`danger` item — Cancel for the remove/close confirms (so a reflexive Enter is a
no-op), while the one confirm whose lead item isn't destructive (the agent-worktree approval's
"Create") keeps its top row. Native: `PaletteModel.initialIndex(for:)` in `push`, plus the
`confirmRemoveBranch` order restored to match the design (Remove-from-sidebar before Delete-worktree,
so even a regressed default can't land on disk-deletion). Design: `paintList` starts a confirm on its
first non-danger row in both `working.html` and `big-picture-design.html` (invariant held). The
`sweepDetachedWorktrees` / `detachWorktree` machinery itself is unchanged — it did exactly what it was
told; the bug was telling it.

## Synth 0.11.0 shipped (build 346)

Minor bump. Headline is the experimental **Tabs** view mode (off by default, one Settings toggle,
losslessly reversible), and riding with it the **worktree-delete safety fix** above — a `.confirm`
frame can no longer open on a destructive row, so a stray ⌘D on a branch or a reflexive Enter can't
`rm` a checkout. Notarized and stapled (zip + dmg each), verified credential-less on the quarantined
downloads (spctl accepted / Notarized Developer ID, staple valid on the app inside the image and the
dragged-out zip). Appcast newest is `sparkle:version` 346 (0.11.0) with an EdDSA signature on the full
download and every delta; 0.10.1 (342) users take an ~880KB `Synth346-342.delta`.

**Released twice, published once.** The first run was killed mid-upload (before the appcast went live)
to hold the build while the delete-safety fix landed — nothing shipped, no tag, installed copies stayed
on 342 throughout. That killed run (build 345) left a stale `0.11.0/345` `<item>` in the local
`releases/appcast.xml`, which `generate_appcast` preserved into the re-run's feed as a broken duplicate
(same `Synth-0.11.0.zip` URL, wrong length + signature, `Synth345-*.delta` URLs that 404). Removed it
surgically from the published feed (346 + 342 the only items, every enclosure signed) — Sparkle always
offered the newer 346 so no installed copy was ever at risk, but a killed release should not be able to
poison the next feed. *Latent pipeline gap worth a `release.sh` follow-up:* regenerate the appcast from
a clean slate each run, or prune items whose deltas moved to `old_updates/`, so an aborted run leaves
no residue.

## Destructive mouse actions go instant, with an undo pill as the net

Closing a session or removing a project no longer stops to confirm. The gesture just does it — the
row soft-deletes immediately and an **undo pill** parks it at the bottom-centre for a window, draining
a countdown bar; until it empties the removal can be brought back. This replaces the old model where
*every* Close/Remove drilled into a ⌘K confirm frame (`requestDelete → confirmFrame`), a friction the
in-code comments already claimed didn't exist ("idle sessions go straight through" — the code never
implemented it). Speed is the product's first principle; a confirm on the most common gesture violated
it, and an undo net is the safer, faster trade.

**Decisions (settled with the user):**

- **Undo lives only for the window.** No archive/restore surface — when the bar empties the removal
  commits and the detached nodes are let go. The pill *is* the safety net, matching a browser's
  "reopen closed tab" feel, not a trash can.
- **Every close raises a pill**, including the frequent session close — consistency over quiet. One
  pill at a time; a new destructive action commits the previous one.
- **Undo = click the pill or ⌘↩** — the same chord that jumps a notification, which now prefers a
  pending undo when one is up and falls through to the notif deck otherwise.
- **Window = 8s** (`UNDO_MS`), longer than the 6s notification drain — destructive actions get extra
  reaction room. The pill pauses its countdown on hover so the cursor can reach *Undo* without the
  window closing under it.
- **The one action that still confirms is deleting a worktree.** Its two outcomes differ irreversibly
  — *Remove from sidebar* (folder + branch stay on disk) vs *Delete worktree* (rm the folder) — so the
  deliberate fork stays. But both arms then soft-delete with the same undo pill ("Removed …" /
  "Archived …"), so nothing is actually torn off disk until the window closes: the confirm picks the
  outcome, the pill guards the seconds after.

**How it's built (reuses existing grammar, no new machinery invented).** A `softRemove(unit, {label})`
replaces the old hard `removeUnit`: it snapshots the unit's DOM anchors (and, for a claude session, its
owned browsers via the ADR-0011 cascade) up front, runs the row-exit animation, detaches the nodes
keeping the refs alive, then settles the tree through the shared `settleAfterRemoval` (the old
removeUnit tail: roll-up, prune/reflow the layout, fall focus up the hierarchy). `pendingUndo.restore()`
slots every node back at its anchor and re-opens it if it was the open session; the commit path just
drops the refs. The pill is an actionable `.fb-toast--undo` that drains the **notif countdown bar**
(`.notif__timer` / `notif-drain`, `--life`) and commits on its `animationend` — bar and timer can't
disagree. Every delete gesture (kebab, tab ×, ⌘W, ⌘D, `d`, and the ⌘K Close/Remove items) funnels
through the one `requestDelete` choke point, so the whole surface changed at once.

Landed in both `working.html` and `big-picture-design.html` (the subset invariant holds — the two
still diff only by title + the browser/simulator demo rows). Runtime-verified by driving the design in
a Synth browser: instant close with the owned-browser cascade, undo via click **and** ⌘↩ both restore
the full set, auto-commit after the 8s window, the worktree fork still confirming, and a fork arm
soft-deleting with the right label. **Native app (`app/`) is out of scope for this design pass** — its
ports already mirror the non-blocking model; porting soft-delete+undo to SwiftUI is a follow-up.

## The native app lands the instant-actions + undo pill

The follow-up above is done — the SwiftUI app now behaves like `working.html`. The choke point
`AppStore.requestDelete` (kebab ×, tab ×, ⌘W, ⌘D, `d`) no longer opens a ⌘K confirm frame; a session
or project soft-deletes instantly and an 8s undo pill (`UndoPill.swift`, `AppStore.pendingUndo`) parks
it at the bottom-centre, draining the notification deck's countdown bar (`NotifTimerBar` grammar) and
pausing on hover. Click the pill or ⌘↩ to bring it back — the chord now beats the notif-deck jump —
else the bar empties and it commits. The ⌘K Close/Remove items reroute the same way.

The three soft mutators (`softCloseSession` / `softRemoveWorkspace` / `softRemoveBranch`) detach the
model reversibly and defer the two irreversible side-effects — `teardownSession` (the PTY/agent
process kill) and `deleteWorktreeFolder` (the on-disk `git worktree remove`) — into the pill's commit
closure, so an undo inside the window leaves processes and the checkout fully intact; `restore` slots
the rows back at their captured indices (a claude session brings its owned browsers with it). Only one
pill is live at a time — a fresh gesture commits the previous. Deleting a worktree keeps its deliberate
remove-from-sidebar / delete-worktree fork; both arms then soft-delete with the pill. The hard
`closeSession`/`removeBranch` stay for the non-gesture paths (a session exiting on its own, a
failed-create rollback); the vestigial `RowMenu` popover is untouched.

Runtime-verified by driving the built app over its control socket: `automation.requestDelete` on a
session left the palette closed (instant, no confirm) and the row gone, the undo pill rendered "Closed
…" with its bar, the owning claude's browser cascaded out with it, `automation.key` ⌘↩ restored the
set, the branch Remove still opened the `Remove main?` fork, and its "Remove from sidebar" arm
soft-deleted with a "Removed main" pill. `working.html` is untouched by the port.

## The undo folds into the notification deck (supersedes the bespoke pill above)

The two entries above shipped the undo as its own bottom-centre toast — `.fb-toast--undo` in the
design, `UndoPill.swift` in the app. That's a third toast primitive competing with the notification
deck, which already carries exactly what an undo needs: a glass card, a draining countdown bar, ⌘↩,
and click-to-act. UI is consistency, so the undo now **is** a notification: a session-less `.undo`
card raised into the same deck, same bottom-left corner, same chrome. The bespoke pill is deleted in
both files (`UndoPill.swift` gone; `.fb-toast--undo` CSS/JS gone).

- **Design (`working.html` + big-picture):** `softRemove` raises a synthetic session-less key into
  `notifState` with `kind: 'undo'`; `notifOrder`/`buildNotif`/`renderNotifs` render it as a
  `.notif--undo` card (undo glyph, "Closed …", the deck's `.notif__timer` bar, `⌘↩ undo` hint).
  Clicking the card or ⌘↩ restores; the bar's `animationend` commits. Ranked ahead of error/input/done
  so a just-triggered undo sits in front.
- **Native (`app/`):** a `.undo` case on `NotifKind`; `softDelete` appends a session-less `InAppNotif`
  (message = label, `iconPath` = `Phosphor.reset`, `life` = 8s) and arms it through the deck's own
  `armDoneToast` drain — whose expiry now runs the stored `undoActions[id].commit`. `NotifCard` renders
  `.undo` as a single-line card with the timer bar and an "undo" hint; the click and ⌘↩ resolve to
  `performUndo`. `pendingUndoNotif` gives ⌘↩ its undo-first priority. `InAppNotif` gained a per-instance
  `life` (the timer bar was hardwired to `doneLife`), so the 8s undo drains at the right rate.

A real consequence of reusing the deck: the undo now inherits the deck's drain brakes — it **pauses on
hover and while Synth is unfocused**, banking its life and resuming on return. That's stricter than the
old always-draining pill, and it's the right behaviour: your undo window waits for you, exactly like a
notification. Verified by driving the built app — the `.undo` card shows in the deck
(`automation.notifs` reports `kind: undo`), ⌘↩ restores it (rows return, deck empties), and a
focus-held drain commits (card auto-dismisses, sessions stay gone). Design verified in a Synth browser
(card in `.notifs`, click + ⌘↩ restore). Subset invariant intact.

## Close-session moves from ⌘D to ⌘W (supersedes the ⌘D binding above and in prior entries)

The verb that closes the current context — the focused sidebar row when the keyboard owns the
sidebar, else the open session — is now **⌘W**, not ⌘D. Nothing else about it changes: it's still the
instant soft-delete + undo-deck flow (no "are you sure" — the deliberate remove-from-sidebar /
delete-worktree fork stays the sole confirm, unchanged), still the one `requestDelete`/`closeContext`
choke point every gesture funnels through. Only the chord moved, so ⌘W now matches the muscle memory
of every tab/session app (⌘W = close this thing) and frees ⌘D. The bare sidebar `d` key is untouched.

**Design (`working.html` + big-picture).** The keydown handler that read `⌘D` now reads `⌘W`; the
tabs-mode `⌘W` branch (close the active tab) is unchanged and still wins first in tabs mode, so the
new binding is the everywhere-else path. Shortcuts sheet General row is `⌘W Close session`; the Tabs
group's `⌘W Close tab` drops its old `⌘D` alias; the ⌘K session **Close** item shows `⌘W`. Subset
invariant held (the two files still diff only by title + the browser/simulator demo rows).

**Native (`app/`).** `closeContext()` now returns `Bool` (did it close anything) so ⌘W can fall
through to the stock macOS ⌘W window-close only when there's genuinely nothing to close (nothing
focused, or Settings open) — mirroring the design's "preventDefault only when there's a row." Because
`WindowGroup` auto-provides a stock ⌘W "Close Window", the local key monitor now owns ⌘W in **all**
modes (the old handler was tabs-only) and consumes it before it reaches the menu, so a real close
never leaks to the window; the File-menu "Close Session" item is rebound to ⌘W for discoverability but
the monitor is the authoritative binding. `Shortcuts.swift`, the palette `Close` kbd hint, and the
`requestDelete`/`TabStrip`/`Palette` comments all track ⌘W. Builds clean.

## Tabs mode: the keyboard cursor is branches-only, stated outright (not inferred from CSS)

In Tabs mode the sidebar is 2-deep — sessions are tabs in the panes, not tree rows — so `J`/`K`
(and `↑`/`↓`) must walk repos and branches only, never a session. The native app (0.12.2) was
leaking the cursor into the now-hidden session rows: pressing `J`/`K` stepped through sessions you
can no longer see in the tree.

Root cause: keyboard-nav membership was never told about Tabs mode. `treeRows()` derived the
navigable list purely from *visual* visibility (`row.offsetParent === null`), and Tabs mode's
`.session { display:none }` only *happened* to make that true in the web prototype — the cursor
skipped sessions by luck, not by rule. Any render path that keeps the rows laid out (the native
app's, which doesn't hide sessions via an offsetParent-nulling `display:none`) has nothing telling
it those rows aren't part of the tree, so the cursor walks them.

Fix: encode the structural rule directly. `treeRows()` now drops `[data-session]` rows up front when
`tabsMode` is on, before the visibility filter — so "in Tabs mode the sidebar is branches-only" is a
stated invariant of the nav model, independent of whatever CSS happens to be hiding the rows. Landed
in both `working.html` and `big-picture-design.html` (subset invariant held — the two still diff only
by title + the browser/simulator demo rows). Verified by driving the design in a Synth browser: with
Tabs on and every repo expanded + a session open (peek active), `J`/`K` walk repos → branches →
Settings and never land on a session; with Tabs off the same tree still exposes all 8 session rows to
the cursor, so the default build is untouched. The native port (`AppStore.tabsMode` /
`SidebarNavigator`) is the follow-up that carries this rule into the app the user reported against.

## Tabs mode: the rest of the sidebar shortcuts stop toggling a branch's invisible disclosure

The same fix-the-cursor pass surfaced its siblings. In Tabs mode a branch is the deepest sidebar row
(its chevron is hidden — "nothing to disclose"), yet every disclosure-driven shortcut still treated a
branch as an expandable group and toggled its now-hidden session accordion: `→`/`←` "expanded"/
"collapsed" an invisible list instead of moving the cursor, `Tab` flipped it, and `Enter`/click did
too — each a silent no-visible-effect action, exactly the "few wrongs" reported.

Same root cause as the cursor leak: each path re-derived "is this an expandable group?" from the raw
`[data-toggle]` markup, which Tabs mode never updated. Fixed with one predicate both files now share —
`canDisclose(row)` = a `[data-toggle]` row that is *not* a branch while `tabsMode` is on (repo heads
still disclose their branches; branches no longer disclose sessions). Every disclosure path routes
through it: the click handler, `activateRow` (`Enter`/`Space`), `toggleGroup` (`Tab`, plus its keydown
guard so `Tab` on a branch is a clean default no-op), and the `←`/`→` repo-resolution. Activating a
branch — which used to toggle nothing visible — now does the useful thing: `openBranch(row)` brings
that branch's tabs on screen (its already-open session if it owns one, else its first) and moves focus
into the content pane, the sidebar's twin of clicking into the branch; an empty branch is a safe
no-op. `a`/`r`/`d` and `⇧J`/`⇧K` never keyed off disclosure, so they already worked and are unchanged.

Landed in both design files (subset invariant held). Driven-verified in a Synth browser, Tabs on:
`→`/`←` move the cursor down/up on a branch with zero disclosure churn; `Tab` on a branch is inert
while `Tab`/`Enter` on a repo head still expand/collapse it; `Enter`/click on a branch opens its tabs
and focuses content (empty branch no-ops); `r` opens the inline rename, `d` the remove-worktree
confirm, `⇧J`/`⇧K` reorder. Tabs off is byte-for-byte the old behaviour — `→`/`Tab`/`Enter` still
disclose a branch's sessions and `J`/`K` still walk into them. Native port carries `canDisclose` +
`openBranch` alongside the `treeRows` rule above.

## The native app lands the tabs-mode sidebar-keyboard fix

The port of both entries above into the SwiftUI app (`Navigation.swift` / `Sidebar.swift`). The gap
was the exact web one: the sidebar *view* was fully tabs-aware (hidden branch chevron, disclosure
forced shut, no session rows) but the keyboard *model* wasn't, so the cursor walked session UUIDs
that had no on-screen row and `→`/`←`/`Tab`/`Enter` toggled a branch's hidden accordion.

Mirrored the design's three moves: `visibleRows` (the native `treeRows`) drops `.session` rows when
`tabsMode` — stated outright, not left to the view happening not to render them; `isToggle` becomes
`canDisclose(_:)`, returning `!tabsMode` for a `.branch` (workspaces still disclose), and every
disclosure path (`expandOrIn`/`collapseOrOut` for `→`/`←`, `cursorIsGroup`/`toggleGroup` for `Tab`,
`activateCursor` for `↵`) routes through it; and a new `AppStore.openBranch(_:)` — factored out of the
branch row's click handler so keyboard `↵` and mouse click share one verb — opens the branch's
remembered-layout first-leaf session (else its first) instead of toggling nothing. Repo-head
disclosure, `a`/`r`/`d`, and `⇧J`/`⇧K` are untouched; a tabs-off build is byte-for-byte prior
behaviour.

`swift build` clean. Driven headless over the control socket on a seeded two-workspace tree: Tabs on,
`↓`/`↑` walk `proj-one → main → feature-x → proj-two → main2 → Settings` and never land on a session
(both directions), and `↵` on `feature-x` opens its tabs (`Claude Two` active, `term two` beside it) —
confirmed by screenshot. Tabs off, the same tree still walks `main → ClaudeOne → termOne → feature-x →
…` into the sessions. `working.html` untouched by the port.

## Synth 0.12.0 shipped (build 353)

Minor bump carrying the single change above: **close-session moves from ⌘D to ⌘W**. Notarized and
stapled (zip + dmg each), verified credential-less on the quarantined downloads — spctl accepted /
Notarized Developer ID, staple valid on the app inside the image *and* the dragged-out zip. Appcast
newest is `sparkle:version` 353 (0.12.0) with an EdDSA signature on the full download and every delta;
0.11.0 (346) users take a ~727KB `Synth353-346.delta`. A clean run this time — no killed pre-publish,
no stale-item residue in the feed. Landing-page download links are unchanged (still the stable
`Synth.dmg` alias), so the marketing site needed no republish.

## Terminal colours: dark rides ghostty's default, light cools to near-white

Two independent terminal-appearance fixes, both merged from feedback branches.

**Dark mode drops the override.** Synth had been painting a bespoke near-black card over the
terminal — its own background/foreground/cursor/selection/palette lines. Claude Code ignores all of
it (it paints its own truecolor dark theme), so the override only fought the surface for no benefit.
In dark mode `TerminalTheme.swift` now emits none of those lines and lets ghostty's own default
scheme stand.

**Light mode cools, and a dead focus ring goes.** Light mode wrapped every terminal in a warm,
yellowish edge that clashed with the cool chrome — two separate causes. In the designs
(`working.html` / `big-picture-design.html`), `.term:focus` still painted the copper accent ring
that decision 004 §4 had superseded with the top-edge focus bar; dead styling, removed, leaving just
the outline suppression. In the app, `Theme.tuiBg` used a warm "paper" cream (`0xF3EFE7`) instead of
the design's cool near-white (`--tui-bg` `#f7f8fa`); aligned to `#f7f8fa`. The light palette earns
its keep against Claude Code's light theme (built for `#fff`), so unlike dark it stays.

## Synth 0.12.1 shipped (build 359)

Patch bump carrying the two terminal-colour fixes above: dark terminal now uses ghostty's default
scheme (no Synth override), and the light terminal cools to a near-white matching the chrome (plus
the stray copper focus ring removed from the designs). Notarized and stapled (zip + dmg each),
verified credential-less on the quarantined downloads — spctl accepted / Notarized Developer ID,
staple valid on the app inside the image *and* the dragged-out zip. Appcast newest is
`sparkle:version` 359 (0.12.1), all 18 enclosures EdDSA-signed; 0.12.0 (353) users take a small
`Synth359-353.delta`. Clean run. Landing-page download links unchanged (stable `Synth.dmg` alias),
so no marketing-site republish.

## Dark terminal keeps the background override (supersedes the 0.12.1 drop)

0.12.1 dropped the dark override *entirely* — no background either. That read the original request
too broadly. The complaint was the bespoke **palette/accent** recolouring fighting Claude Code's own
dark theme, not the dark surface itself. With the background gone too, `window-padding-color =
background` filled the 8×6px padding band with ghostty's *default* dark grey — lighter than the app's
near-black frame (`--panel #191b1f`, `--tui-bg #121317`). The result: a pale halo around Claude
Code's content sitting inside a darker frame, surface and border no longer one continuous card.

The fix keeps the background override and nothing else. In dark mode `TerminalTheme.swift` now emits
a single `background = 121317` line (matching `--tui-bg`) and lets foreground, cursor, selection, and
the ANSI palette ride on Claude Code's own theme. The padding band goes back to the near-black frame
colour — surface flush with border — while no colours get fought. Light mode is unchanged.

## Synth 0.12.2 shipped (build 360)

Patch bump carrying the dark-terminal background correction above: the dark terminal surface (and its
padding band) is back to the near-black `#121317` frame colour, with foreground and palette still
Claude Code's own. Notarized and stapled (zip + dmg each), verified credential-less on the quarantined
downloads. 0.12.1 users take a small delta. Landing-page download links unchanged (stable `Synth.dmg`
alias), so no marketing-site republish.

## Every in-app notification stacks — nothing replaces (supersedes "one pill at a time")

In-app notifications now stack like a browser's: a new one joins the deck rather than evicting whatever
is still on screen, and each fades out on its own timer. This reverses the **"One pill at a time; a new
destructive action commits the previous one"** decision from the *Destructive mouse actions go instant*
entry above — the user's principle is that a notification should never silently swallow an earlier one
you haven't seen. Fixed at the two roots where a notification could replace rather than stack, both in
`working.html` and `big-picture-design.html` (subset invariant held — the two still diff only by title +
the browser/simulator demo rows):

- **Confirmation toasts (`.fb-toast` — "On it", "Handed to Mail").** `flashToast` opened by wiping every
  existing toast (`querySelectorAll('.fb-toast').forEach(remove)`) and shared a single module-level
  `toastTimer`, so a second toast both deleted the first's node and cancelled its dismissal — pure
  replace. Now toasts live in a lazily-created bottom-centre `.fb-toasts` flex column (`column-reverse`,
  so the newest slides in at the bottom and older ones ride up); each toast owns its own dismissal
  `setTimeout` and removes only itself. No shared timer, no eviction.
- **Delete/close undo cards.** The undo card already rendered through the notification deck, but its
  state was a single `pendingUndoKey`, and `softRemove` called `commitPendingUndo()` up front — so a
  second delete committed the first's undo before its 8s bar drained. This was the user's actual
  symptom ("delete a session… it didn't seem to stack"). Now `pendingUndos` is a `Set`; each removal
  parks its own card and each card commits/undoes its own key (the card's click → `performUndo(key)`,
  its bar's `animationend` → `commitPendingUndo(key)`). ⌘↩ still undoes, now targeting the front-most
  (newest) undo card. Several removals can wait for undo at once, each on its own countdown.

Design verified by driving it in a Synth browser: three feedback submits left three stacked "On it"
toasts co-resident (`column-reverse`, all faded in) instead of one; deleting two workspaces parked two
independent undo cards in the deck, and undoing the newest restored exactly that workspace while the
other card kept standing.

**Native app (`app/`) landed in the same change.** The Swift side only ever had the *undo* root — its
confirmation toasts already append to the shared deck (`notifs`), which stacks, so there was no
separate replace-on-toast primitive to fix. The undo was single via two things: `softDelete` opened
with `commitPendingUndo()` (evicting the previous), and undo resolution picked `notifs.first(kind: .undo)`.
The per-card drain machinery (`armDoneToast` / `undoActions` / `notifDismissTasks`, all keyed by notif
id) was already per-id and shipped, so stacking needed only: drop the `commitPendingUndo()` in
`softDelete`; replace the shared `resolveUndo`/`performUndo()`/`commitPendingUndo()` with a per-id
`performUndo(_ id:)` (card click → `performUndo(notif.id)`); and repoint `pendingUndoNotif` at the
front-most undo (`notifOrder.first { .undo }`) so ⌘↩ undoes the newest. `swift build` links clean;
change is a minimal, faithful mirror of the browser logic on top of the already-tested per-id drain.

## Tabs: double-click a tab to rename, right-click for its ⌘K menu

Two direct-manipulation gestures on the experimental Tabs strip, each the horizontal twin of a
gesture the sidebar row already has — so a tab is now as editable as the row it stands in for. Both
land only in tabs mode; tabs-off is untouched.

**Double-click → inline rename.** Double-clicking a tab's name swaps the label for an editable field
with the whole name preselected — ↵ commits, Esc reverts, clicking away commits — exactly the sidebar
row's `r` rename, keyed on the same `renamingRowID`/`renameText` store fields and the same global
Return/Esc monitor, so committing the open session's tab updates its pane title for free. Applies to
both a lone tab and a bonded split-member (cluster) chip.

**Right-click → the session's ⌘K frame.** Secondary-clicking a tab opens the command palette pinned
to that session — the same `openRowActions(.session)` the sidebar row's ⋯ kebab / right-click opens,
so a tab surfaces the exact grouped Session verbs (Rename / Close, plus the branch/project search
groups) with no bespoke per-tab menu to keep in sync.

**Design (`working.html` + big-picture).** A `startTabRename` mirrors `startRename` on the tab's
`.tab__name` (contentEditable, select-all, commit through `renameUnit` so the sidebar row + open pane
stay in sync); `content` gains `dblclick` → rename and `contextmenu` → `openRowActions` handlers, both
`tabsMode`-guarded. A click inside a renaming tab just moves the caret (the tab-open and tab-drag paths
skip a `.tab__name[contenteditable]`), so editing never re-opens or drags the tab. Subset invariant
held (the two files still diff only by title + the browser/simulator demo rows).

**Native (`app/`).** `TabChip`/`ClusterChip` gain a `renaming` branch that shows the sidebar's
`RenameField` (made internal) in place of the button, plus `.onDoubleClick { beginRename(.session) }`
and `.onSecondaryClick { openRowActions(.session) }`. A new `DoubleClickCatcher` — the double-click
twin of the existing `SecondaryClickCatcher` — is a transparent AppKit overlay whose `hitTest` claims
only the second click of a plain left double-click and declines everything else, so single-click open,
the tab drag (reorder / pair / split), and the close button all pass straight through. While a tab is
renaming its drag stands down (`highPriorityGesture … including: .subviews`), so drag-selecting the
name in the field never hijacks into a tab drag — the app twin of the mock's contentEditable guard.
Builds clean.

## Clicking a sidebar row hands the keyboard to the sidebar

Clicking a row in the tree now makes the sidebar keyboard-active, not just visually selected. Before,
a click moved the selection ring but the keyboard stayed wherever it was — clicking a session ran
`focusContent()`, which focused the pane's terminal/composer, so the very next `r`/`d`/`a`/`j`/`k`
went to the content pane instead of the row you just clicked. The tell was subtle: you'd click a
branch, press `R` to rename, and nothing happened in the tree.

The gate for whether nav keys act on the tree is line-of-sight simple — the keydown handler bails if
`document.activeElement` lives inside `.content`. So the fix is to stop pulling the keyboard into the
content pane on a click. A new `handToSidebar(row)` mirrors ⌘0's `focusSidebar()`: un-collapse the
sidebar, blur any content element that holds focus, add the `kbd` ring class, and select the clicked
row. Both branches of the `nav` click handler (the `[data-toggle]` group heads — branches/worktrees —
and the `[data-session]` leaves) route through it. A clicked session still *opens* in the content pane
(`openSession` renders it, marks it read, derives the pill); the keyboard just no longer follows it
there. To dive into the pane and type, press ↵ (which runs `activateRow` → `openSession` +
`focusContent`) or ⌘1 — the explicit "commit into it" gestures, unchanged.

This lands the file-tree model everyone already knows from Finder/VS Code: single-click selects and
previews with the keyboard staying on the tree; ↵ opens and dives in. The `kbd` ring is still cleared
by the next `mousemove` (the resting state stays mouse-clean) and re-armed by the next nav keypress —
that behaviour is untouched; what changed is only *where the keyboard points* after a click. Both
designs carry it (subset invariant holds — the two files still diff only by title + the
browser/simulator demo rows).

**Native (`app/`).** A `handToSidebar(_ id:)` store method mirrors the web helper — it's ⌘0's
grammar (`focusSidebar()` to release first responder, `keyboardActive = true`, cursor onto the row)
aimed at a specific row. The three sidebar click sites route through it: workspace and branch heads
call `handToSidebar` directly; session rows call a new `openFromSidebar(_:)` (open + handToSidebar).
The experimental tabs path (a branch click opening its tab) is untouched — it still dives into the
pane via `focusContent`. The subtlety the web didn't have: `TerminalHost.makeNSView` deliberately
grabs first responder for the shell on mount (via `DispatchQueue.main.async`), which fires *after*
the click closure and re-stole focus from the sidebar — so a session click still landed the keyboard
in the terminal. Fixed with a one-shot `suppressShellFocusOnOpen` flag: `openFromSidebar` raises it,
the terminal mount consumes it (skipping that one focus-grab), and any dive-into-content path
(`focusContent`) clears it, so ↵/⌘1 still focus the shell. Driven in the real app (non-tabs): clicking
a terminal session opened it in the pane while the keyboard stayed on the tree — `r` renamed the row
inline and the shell prompt stayed clean; pressing ↵ then typed into the shell. Builds clean.

## Synth 0.13.0 shipped (build 376)

Minor bump rolling up everything since 0.12.2. The headline is the interaction change to the default
experience — **clicking a sidebar row now hands the keyboard to the sidebar** (open the session in the
pane, but `r`/`d`/`a` and `j`/`k` act on the clicked row at once; ↵ or ⌘1 dives in to type) — plus new
experimental-Tabs gestures (double-click a tab to rename, right-click for its command menu) and the
tabs-mode keyboard-nav fix (sidebar shortcuts stay on branches instead of flipping a hidden session
list). Two fixes ride along: in-app **notifications now stack** rather than a new one evicting an
unseen card, and the **terminal re-scales** when the window crosses monitors of different pixel density
(no more staying blurry until the next relayout).

Notarized + stapled (zip + dmg), verified credential-less on the quarantined downloads: spctl accepted
/ Notarized Developer ID on both the dmg (and the app inside it) and the zip's app, staples valid.
Appcast newest is `sparkle:version` 376 / `0.13.0`, all 18 enclosures EdDSA-signed, build-376 deltas
published against 342/346/353/359/361. Clean run — no killed pre-publish, no feed residue (16 superseded
delta files rotated into `old_updates`). Landing links unchanged (stable `Synth.dmg` alias), so no site
republish.

## Archive replaces Delete worktree, with a background clean-up behind it

**Supersedes** this file's earlier entries at "No archive/restore surface… not a trash can" and the
delete-fork justification below it, plus ADR-0007's "UI-only, labelled Remove never Delete" (dated
notes appended to both ADRs). Those decisions were correct for a world where removing a branch row
never touched disk. This feature changes that, so they had to move.

**The verb.** A branch row **archives**: it leaves the sidebar instantly with the usual 8-second undo
card, its folder untouched, restorable indefinitely from ⌘K → `Archived`. The two-armed
"Remove from sidebar / Delete worktree" confirm fork is gone; what survives of it is
`Delete worktree now`, one level down in ⌘K's branch frame, red, and confirming from every surface.
`Remove` is now scoped to project rows only.

**Archive confirms nowhere.** This reverses the shape originally proposed (silent on the row,
confirming in ⌘K). The proposal correctly identified that ⌘K's target is *inferred* — `contextRow()`
falls through pinned → focused row → open session. The first cut answered that by putting the branch
name in the label ("Archive `<name>`"); that was reverted, because every neighbouring action in that
group is a bare verb with the target in `ctx`, and one item breaking the pattern is a worse problem
than the one it solved. The group heading and ctx already name the branch. Against the asymmetry, it
taxed the deliberate path (open ⌘K, type, read, ↵)
while leaving the reflexive ones free — a hover-revealed 14pt glyph beside the `+`, bare `d`, and
**⌘W**, the most habituated chord on macOS — and it had no answer at all for which bucket the
keyboard paths fell into. Apple's invariant is the opposite one: Mail archives from swipe, toolbar
and ⌘⌃A with no dialog, and the dialog is bolted to Empty Trash. So: the dialog follows
irreversibility, never the entry surface.

**The copy stays out of the way.** The first cut put the retention clause in every string ("folder
deletes in 7 days once it's merged and clean") on the theory that "archive" means "kept forever"
elsewhere and the gap had to be narrated. Cut on review: it turned a one-word action into a running
commentary on machinery the user has no decision to make about. To the user this is one idea — the
row is put away and it can come back — so the undo card reads `Archived <name>`, the Archived list
reads `archived 3d ago`, and the conditions go to `os.Logger` plus a `reason` slug on
`automation.archiveStatus` (which is what the gate asserts on, so the tests keep their teeth without
the UI carrying jargon).

**Undo cards now wear the thing you acted on.** They all shared one reset-arrow glyph, which says
"undo" a second time — the ⌘↩ hint already does — while saying nothing about what just left. A
closed session shows its own mark via `SessionIcon` (Claude, OpenCode, terminal, browser), an
archived branch the archive box, a deleted worktree the trash can, a removed project a folder.

**Two of the proposed sweep conditions were unsafe as stated**, both measured against this machine:
- *"no uncommitted changes"* — the natural reading is tracked-only, and tracked-only is blind to
  the files that matter most. The live `browser-phase-01` worktree holds `?? app/Sources/CEFBridge/`,
  `?? app/Sources/Synth/BrowserEngine.swift`, `?? app/Sources/Synth/CEFBrowserEngine.swift` and four
  more untracked source paths: real work, in no commit, on no remote, in a 2.6 GB tree. (That
  worktree also carries a tracked ` M app/Package.swift`, so `git diff --quiet HEAD` does catch *it* —
  drop the one tracked edit and the same folder goes silently sweepable with all the source still
  untracked.) The check has to be `status --porcelain=v2 -uall` blocking on `?` records, not just
  `1`/`2`/`u`.
- *"no open PR — merged or closed is fine"* — inverted twice over. CLOSED-unmerged means a human
  rejected the work and the commits may exist nowhere else; and `pr == nil` was indistinguishable
  from being offline, because `PRService` failed quiet to `[:]` on missing `gh`, non-zero exit and
  unparseable JSON, `Store` then cleared `branch.pr`, and `pr` is never persisted — so it was nil for
  every branch on every cold launch. `PRService.pullRequests` now returns `[String: PRInfo]?` where
  nil means *couldn't ask*, and relevance requires an affirmative `.merged` (or, for repos with no
  GitHub at all, `merge-base --is-ancestor HEAD origin/HEAD`, so those aren't silently inert forever).

**What actually gates a delete** (`ArchiveSweeper.swift`), against one invariant: *every byte in this
folder is reconstructible from a remote or the repo's shared object store, without the user doing
anything clever.* The load-bearing check is `rev-list --count HEAD --not --remotes` — deliberately
not `@{upstream}`, because `autoSetupMerge` leaves branches tracking `origin/main`, so "ahead 3" only
means "not in main", and several managed worktrees have no upstream at all. Around it: not a
Synth-created path, primary checkout, tracked changes, untracked files, in-progress rebase/merge/
cherry-pick, detached HEAD (its reflog is the only ref), a locked worktree, a nested worktree or repo
(**measured live**: `feedback-onboarding/.worktree/{palette-agent-first,tour-native}` hold 4 unpushed
commits an `rm -rf` of the parent would take), precious ignored files that *differ* from the parent
repo's copy, dirty submodules, attached sessions, a live process cwd inside (one `lsof -d cwd`, never
`lsof +D`), a worktree another live Synth claims, symlinked paths, and post-merge commits. Any probe
that fails or times out blocks — `runChecked` merges stderr into stdout, so a `fatal:` is
indistinguishable from porcelain by content, and inferring clean from a failed probe is how a sweeper
eats work. A stash naming the branch is a **warning**, not a blocker: stashes are repo-global and
survive the folder, so blocking would make one forgotten stash permanently unsweepable.

**Nothing is ever deleted by the predicate.** The sweeper's terminal act is a rename to
`.archived-<name>-<epoch>-<id>`, and `GitService.reapHeldWorktrees` deletes it 14 days later reading
nothing but that epoch. So no bug in any condition can reach an irreversible act. Crucially the hold
does **not** prune: pruning drops `<repo>/.git/worktrees/<name>/` — the index and the HEAD reflog —
which would make the hold recover files but not git state. `pruneWorktrees` also gained a reachability
guard, because prune is repo-global and an unmounted volume looks exactly like a deleted folder to it.

**Cadence:** 300s foregrounded / 1800s behind an editor, first tick at +90s, 7-day grace (Settings:
Never / 7 / 14 / 30), two clean evaluations ≥24h apart, 3 holds per tick and 10 per launch, and a
bulk brake — more than 5 eligible at once and it holds none, raising a `Review` card instead. A
backwards clock rewrites `archivedAt` rather than manufacturing an expired grace. In-process only, no
launchd, deliberately: a headless process deleting folders with no UI attached is a different product.
Off by default for one release; `archive_sweeper_kill` is the emergency stop and is read as a KILL
flag, never an ENABLE one (`Analytics.isEnabled` returns false when analytics is opted out, so as an
enable flag a privacy-conscious user would silently lose the feature).

**The `.mcp.json` dirt, fixed at the cause.** Every managed worktree showed ` M .mcp.json` because the
file was committed by an agent in `da7f902` — one day *after* 2026-07-05 said not to — while
`MCPInstaller` rewrites it on the 4s autosave with channel-dependent content. `git rm --cached` plus a
`.gitignore` line. Deliberately *not* encoded as an exception in the sweep predicate: teaching the
thing that authorises `rm` to ignore Synth's own dirt is permanent debt on the wrong file. The
structural half — `--mcp-config` / `OPENCODE_CONFIG` so nothing is ever written into a worktree — is
**not** in this change; it is gated on an unresolved question about whether `OPENCODE_CONFIG` merges
with or replaces a user's global opencode config, and clobbering that would be worse than the dirt.

**Also fixed in passing, on their own merit:** the undo label for the arm that actually `rm`s a folder
said `"Archived <name>"` and now says `"Deleted"`; `refreshPullRequests` had no throttle at all, so
rapid ⌘-tab spawned unbounded `gh` subprocesses (two network calls each, per workspace); `runChecked`
gained an optional timeout, without which one `git status` against a hung network volume leaks a
cooperative-pool thread per tick forever; the dead `rowMenu(for:)` (zero call sites) went, since it
was where a future reader would re-derive the pre-archive vocabulary.

**Gated by a new harness suite** (`app/harness/agents/t9_archive.py`, 23 checks, in `run.sh`). It
builds a sandbox repo with a real bare `origin` and one worktree per hazard, then drives a real
Synth over the control socket with every clock compressed — `automation.archiveBranch` /
`archiveSweep` / `archiveStatus` / `archiveRestore` / `notifDrain`, plus
`SYNTH_ARCHIVE_{GRACE,TICK,HOLD,EVAL_GAP}_SECONDS`. The suite's centre of gravity is the *refusals*:
a sweeper that reclaims a merged, clean, pushed worktree is easy, and each of the six folders it
must refuse is a data-loss bug if it regresses. It also pins the undo semantics (nothing is
committed during the window), the two-evaluation rule (the first sweep holds nothing), the
rename/restore round-trip (restored worktree is registered and NOT prunable — proof the hold didn't
prune), and both reaper directions.

**Three real bugs the suite caught, none of which the build would have:**
1. *The sweeper was inert on every user repo.* Synth writes `.mcp.json` + `opencode.json` into every
   managed worktree and no user repo gitignores them, so every candidate reported "untracked files —
   kept" — including the merged, clean, fully pushed one. Fixed with `isUnmodifiedSynthConfig`, which
   discounts those two files only when the user has put nothing of their own in them (structural, not
   a filename allowlist, so a hand-edited `opencode.json` still counts as the user's work).
2. *The offline fallback never fired.* Relevance named `origin/HEAD` outright, and that ref is absent
   more often than expected — a bare remote whose own HEAD was never set, a hand-added remote. A
   missing ref makes `merge-base` exit 128, which reads as "couldn't tell", so every such repo fell
   through to the `gh` path and reported "can't reach GitHub" forever. Now resolved through
   `GitService.defaultBase`, the same resolution the create path uses.
3. *The pre-rename re-check disagreed with the evaluation.* It duplicated the untracked test without
   the carve-out, so candidates passed evaluation and then silently failed at the last step — a
   sweeper that decides to act and never does. Both paths now share `ArchiveSweeper.isSettled`.

`swift build` clean; `diff working.html big-picture-design.html` still exactly 20 lines; t9 passes
three consecutive runs. The existing gate's `t3_notifs`/`t7_regression`/`t4a_mcpconfig` failures were
verified identical on a clean build of `HEAD` — pre-existing, not introduced here.
