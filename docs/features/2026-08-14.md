# 2026-08-14

## 0.31.1 ships — the click is clean

Patch release carrying the flicker fix above (2026-08-13): the caret's first paint is its
final position, so clicking into a wrapped block no longer shows a one-frame leap. Found by
the user in shipped 0.31.0 minutes after release; reproduced by frame-walking the reveal
transition, fixed by sizing the editor view's viewport synchronously at reveal so the
wrap-aware mapping precedes the first paint, pinned by a frame-walking test. 167 tests.

`CFBundleVersion` 590, tag `v0.31.1`; dmg + zip notarized, stapled, verified credential-less
under quarantine (spctl accepted / Notarized Developer ID on both, the app inside the image
validating on its own); appcast newest 0.31.1 at 590, `edSignature` on all 18 enclosures.
No `synth-site` push — download links unchanged.

## The simulator speaks the browser's language

The browser pane had grown a whole vocabulary — a framed card, a toolbar of hardware-sized
buttons, one identity field that names what is loaded and reopens the "open…" surface, a mono
badge for the number that moves while you drive, tools that light while their mode is on, a
docked panel, notices over the view, comment mode. The simulator had none of it: a device
frame floating in an empty pane. Same app, two different products.

So the vocabulary was extracted rather than copied. Everything a second surface would want is
now named `pane-*` and shared by both: `pane-surface`, `pane-bar`, `pane-btn`, `pane-omni`,
`pane-badge`, `pane-field`, `pane-rec`, `pane-drop`, `pane-notice`, `pane-dock`. Comment mode
moved out of the browser entirely into `wireComments(host, openEl, hooks)` — pins, the scope
you widen, the batch, the island, delivery, all of it — with the surface handing in only what
is its own: whether there is anything to comment on, the *place* a comment belongs to and how
to get back to it, the view's scale, and who receives a batch nobody owns.

The simulator then says the same things in hardware. Left cluster: Home, Lock, Rotate — press,
rather than back/forward/reload. Identity field: the bundle id of the running app, opening the
same floated launcher the browser's address drop is, over Installed instead of Recent, taking
a bundle id or a deep link. Badge: the device's viewport in points, which moves when you
rotate. Then comment mode, the device log (Console · Network · Views) docked exactly where
DevTools docks, and hand-off to Simulator.app. ⌘K carries a Device group beside the browser's
Page group; ⌘L, ⌘R, ⌥⌘I keep their meanings, and ⌘⇧H / ⌘⇧R are home and rotate.

Two decisions worth keeping. **The app's screen is never rebuilt to drive the device** — home,
lock and rotate leave it standing (hidden, or re-laid-out from the frame's own custom
properties), so a comment left on the app is still on it, still anchored to its element, when
you come back; only a launch replaces it, because a launch really does. And **a simulator is
never adopted**: a browser re-nests under the agent its comments spawn, but a simulator's row
carries the booted device's own liveness, and ownership would take that indicator's slot — the
batch still spawns the agent that receives it.

## The app's simulator pane grows the same chrome

The design change above, landed natively. `PaneChrome.swift` now owns what both driven surfaces
share — the bar strip, the 26×26 icon button and its press, the mono badge, the "open…" field, its
drop and its rows — and `BrowserPane` builds from it with every number it already had. That is the
Swift half of the HTML's `.browser__bar` → `.pane-bar` rename: one vocabulary, two speakers.

The simulator's bar became press · what is running · the size it runs at · the tools. Home, Lock and
Rotate are glyphs in a tight cluster; the identity field names the running app's bundle id and opens
a launcher over the device — the same floated panel the browser's address drop is, listing what
`simctl listapps` says is really installed, and taking a bundle id or a deep link; the badge carries
`iOS 18.6 · 393 × 852` and follows rotation. ⌘K grew a **Device** group beside the browser's Page
group (Open app or URL, Relaunch, Home, Lock, Rotate, Shake, Copy screenshot, and comment mode),
⌘L / ⌘R / ⌘⇧H / ⌘⇧R claim the window while a simulator is open, and the shortcuts sheet says so.

Shake, screenshot, launch and open-URL already existed as agent verbs with no way for a person to
reach them; they have hands now.

Two things in the design were deliberately not ported. The docked device log has no data source in
the app, and the browser's DevTools isn't docked either — it is CEF's own window — so a skeleton
panel would have been a drawing of a feature. And "Open in Simulator.app" stays out because
ADR-0015's whole point is that Synth reads the framebuffer itself and never launches it.

Found on the way: `deviceInfo` was resolved once at init from a cache the fleet refreshes in the
background, so a session opened moments after launch never learned its runtime. Invisible while the
bar printed a name from the same nil; the badge would have shown half a fact.
