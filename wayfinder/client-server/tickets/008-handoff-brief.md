---
id: 008
title: Package the handoff brief
type: task
status: open
claimed_by:
blocked_by: [004, 005, 006, 007]
---

## Question

The destination. Assemble the spec + handoff brief (to assets): the locked boundary and lifecycle
rules, the v1 API surface and wire protocol, screen-state decision, the scripts litmus as an
acceptance gate, *keystroke feels native, verified* as an acceptance criterion, and the migration
slice list from [007](007-swift-migration-order.md). Framed for implementation as a separate
effort. Human reviews before it's called done; feature entry appended to the ledger
(`docs/features/` + `FEATURES.md`).

## Resolution
